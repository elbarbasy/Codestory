Note: lenis dependency removed to fix Vercel build error (lenis@0.2.29 not found).

If you later want lenis for smooth scrolling effects, we can re-add a validated version (or use a CDN) after checking the available package versions. For now removing it lets Vercel install dependencies successfully.
