/*
  Lightweight client-only script to lazy-load the RequestQuoteModal React component into a page.
  This is compiled as a small ES module that the server-rendered pages import with dynamic script.
*/

export default function mountRequestQuoteModal(){
  // This file is a placeholder because Next.js dynamic import from inline script would require extra bundling steps.
  // The actual React component is available at /src/components/RequestQuoteModal.tsx and will be hydrated by Next.js.
}
